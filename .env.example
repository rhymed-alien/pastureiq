# Copy this file to ".env" and fill in your keys. The real .env is gitignored
# and must NEVER be committed. This template is safe to commit.

# Anthropic API key — plain-English recommendation module (Mini-project 3)
ANTHROPIC_API_KEY=

# Google Earth Engine project id — only if/when you add satellite NDVI
# EE_PROJECT_ID=
